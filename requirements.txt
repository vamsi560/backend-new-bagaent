# Curated requirements compatible with Python 3.12 (2025-09-23)
# Trimmed to packages referenced in the codebase and common extras used by LangChain/HF.
# Review optional heavy packages (torch, faiss) for your target environment (CPU vs GPU).

flask==2.3.3
flask-cors==4.0.0
requests==2.31.0
python-docx==0.8.11
PyPDF2==3.0.1
azure-communication-email==1.0.0
sqlalchemy==2.0.23
qdrant-client==1.7.0
sentence-transformers==2.2.2
python-dotenv==1.0.0
psycopg2-binary==2.9.9
gunicorn==21.2.0

# LangChain ecosystem (install core and community extras per your use)
langchain==0.0.337
langchain-core==0.1.21
langchain-community==0.1.34

# Hugging Face / embeddings / transformers stack
transformers==4.33.2
torch==2.2.2
huggingface-hub==0.18.1
hf-transfer==0.1.6

# LangChain community document loaders / embeddings
langchain-embeddings==0.0.19
langchain-llms==0.0.20

# Vector DBs (qdrant already included)
pinecone-client==2.1.0
weaviate-client==3.20.0
faiss-cpu==1.7.4

# Azure / Microsoft integrations used across files
azure-identity==1.14.0
msal==1.27.0
azure-devops==2.0.0b3
msrest==0.7.1
azure-core==1.28.0

# Utilities used by the project
python-magic==0.4.27
Pillow==10.0.0
pdf2image==1.16.3
docx2txt==0.8

# Analytics / data
pandas==2.2.2
numpy==1.26.4
networkx==3.2
plotly==5.16.0

# Background / caching
celery==5.5.0
redis==4.7.0

# Optional model backends / providers
openai==1.32.0
google-generativeai==0.13.0

# Security / auth
PyJWT==2.8.0
bcrypt==4.0.1

# End of curated requirements
flask==2.3.3
flask-cors==4.0.0
requests==2.31.0
python-docx==0.8.11
PyPDF2==3.0.1
azure-communication-email==1.0.0
sqlalchemy==2.0.23
qdrant-client==1.7.0
sentence-transformers==2.2.2
python-dotenv==1.0.0
psycopg2-binary==2.9.9
gunicorn==21.2.0 
